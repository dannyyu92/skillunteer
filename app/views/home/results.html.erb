<p>Here are the most relevant matches within zipcode <%= @zipcode %> ± 100:</p>

<% @top_charities.each do |x| %>
  <div class="charity">
    <p>
      <strong>
        <%= x.name %>
      </strong>
      <%= link_to "Link", x.link %>
      <% if user_signed_in? %>
        <%= link_to "Save", "/" %>
      <% end %>
    </p>
    <p>
      <%= x.mission %>
    </p>
  </div>
<% end %>
