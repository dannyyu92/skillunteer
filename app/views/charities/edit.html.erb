<h2 class="page-title">Editing: <%= @charity.name %></h2>

<%= render 'charities/form' %>

<%= link_to "Delete #{@charity.name}", charity_path(@charity), method: :delete, confirm: "Are you sure you wish to delete?" %>
